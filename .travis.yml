
sudo: required
services: 
  - docker

before_install: 
  - docker pull node:8-onbuild

install:
  - docker build -t probot-hello .

script:
  - docker run --rm probot-hello npm run test
  - docker run --rm probot-hello npm run lint

deploy:
  provider: heroku
  api_key: ed1fc29f-5b56-4da7-b129-fb6fb5228f22 
  on: heroku_prod
  app: probot-malintha1996


